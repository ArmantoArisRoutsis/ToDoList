<%- include("header") -%>
<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
        <a type="submit" class="nav-link" href="/">Default List</a>
            <% allLists.forEach(function(eachList){ %>
                <a type="submit" class="nav-link" href="/<%= eachList.name %>"><%= eachList.name %></a>
            <% }) %>
        <form class="list" action="/newList" method="POST">
            <form class="list" action="/newList" method="POST">
                <input type="text" name="newList" placeholder="Create a new List" autocomplete="off">
                <button class="new-list-button" type="submit" name="list" value ="<%= listTitle %>">+</button>
            <form class="list" action="/newList" method="POST">
        </form>
    </div>
</div>

</nav>

    <div class="box">
        <h1><%= listTitle %></h1>
    </div>
    
    <div class="box">
        <% allItems.forEach(function(item){ %>
        <form action="/delete" method="POST">
            <div class="item">
                <button class="delete-button" type="submit" name="deleter" value ="<%= item._id %>"><div class="minus"></div></button>
                <p><%= item.name %></p>
            </div>
            <input type="hidden" name="listName"  value ="<%= listTitle %>"></input>
        </form>
        <% }) %>
        <form class="item" action="/" method="POST">
            <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
            <button class="add-button" type="submit" name="list" value ="<%= listTitle %>">+</button>
        </form>

    </div>

<%- include("footer") -%>